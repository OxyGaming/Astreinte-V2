# dependencies
node_modules/
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# misc
.DS_Store
*.pem
.vercel

/src/generated/prisma

# Build artifacts
tsconfig.tsbuildinfo

# SQLite database files (ne pas versionner)
*.db
*.db-shm
*.db-wal
*.db-journal
prisma/dev.db

# Données métier sensibles (contacts réels, postes, procédures opérationnelles)
prisma/data/
# Sauvegardes horodatées (générées par npm run db:backup)
prisma/data-backup/

# Scripts d'injection one-shot (données internes, hors périmètre du repo).
# Note : prisma/export.ts EST versionné (script générique, sans secret) —
# voir commit `chore(prisma): versionne export.ts`.
prisma/inject_*.py
prisma/scan_annuaires.ts

# Fichiers uploadés temporairement (imports admin)
uploads/
tmp/
