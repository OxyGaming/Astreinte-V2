// Ce fichier est conservé pour compatibilité — l'authentification admin
// est désormais unifiée via le cookie front-office (User.role === "ADMIN").
// Voir src/lib/admin-auth.ts pour la vérification serveur.
export {};
